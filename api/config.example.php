<?php
/**
 * Локальные настройки бэкенда. Скопируйте этот файл в api/config.php
 * (он исключён из git) и раскомментируйте нужные строки.
 * Всё необязательно: без config.php работают значения по умолчанию.
 */

// Папка данных. По умолчанию: <папка сайта>/../vtd-data, если она существует
// и доступна на запись, иначе <папка сайта>/data.
// define('VTD_DATA_DIR', '/var/www/u1234567/data/vtd-data');

// Секрет для подписи токенов и хеширования IP (минимум 32 случайных символа).
// Если не задан, генерируется автоматически при первом запросе и хранится в папке данных.
// define('VTD_SECRET', '');

// Хосты, с которых принимаются отзывы. Текущий хост запроса добавляется автоматически.
// define('VTD_ALLOWED_HOSTS', ['vovrenyuk.ru', 'www.vovrenyuk.ru']);

// Адреса прокси, чьим заголовкам X-Forwarded-For / X-Real-IP можно верить.
// define('VTD_TRUSTED_PROXIES', ['127.0.0.1', '::1']);

// Хеш пароля админки. Имеет приоритет над паролем, заданным через саму админку.
// Получить: php -r "echo password_hash('ваш пароль', PASSWORD_DEFAULT), PHP_EOL;"
// define('VTD_ADMIN_PASSWORD_HASH', '');

// Часовой пояс для дат отзывов.
// define('VTD_TIMEZONE', 'Europe/Moscow');
